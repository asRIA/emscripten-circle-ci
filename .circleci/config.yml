# Python CircleCI 2.0 configuration file
#
version: 2
jobs:
  build:
    docker:
      - image: trzeci/emscripten
      
    working_directory: ~/repo

    steps:
      - run:
          name: run tests
          command: |
            ls -la /
            pwd
            ls -la
            emcc --version
